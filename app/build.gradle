apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'MyAndroidKey'
            keyPassword '<tkrf1991'
            storeFile file('C:/Users/Roman/AndroidKeystore/android.jks')
            storePassword '!Vexfxf637!'
        }
    }
    compileSdkVersion 22
    buildToolsVersion '21.1.2'
    defaultConfig {
        applicationId "ru.romanzes.lezginka"
        minSdkVersion 10
        targetSdkVersion 22
        versionName '1.0.3'
        signingConfig signingConfigs.config
        versionCode 4
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:22.2.0'
}
